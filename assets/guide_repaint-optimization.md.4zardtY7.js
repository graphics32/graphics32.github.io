import{_ as a,b as t,e as i,a8 as s}from"./chunks/framework.C-MVLAPI.js";const n="/assets/rp_001.Cl3CDlpx.png",o="/assets/rp_002.db5YKSNo.png",r="/assets/rp_003.CQUbXq0D.png",l="/assets/rp_004.B04Ycps8.png",h="/assets/rp_005a.CSA4Jfw6.png",c="/assets/rp_005b.jmuyDl3n.png",w=JSON.parse('{"title":"Repaint optimization","description":"","frontmatter":{"date":1788304505000,"authors":[],"tags":[],"head":[]},"headers":[],"relativePath":"guide/repaint-optimization.md","filePath":"guide/repaint-optimization.md","lastUpdated":1788304505000,"timestamp":1788304505000,"datetime":"2026-09-01T23:15:05.000Z","contributors":[{"commits":1,"email":"anders@melander.dk","name":"Anders Melander","avatar":"https://gravatar.com/avatar/a6bff52a3ccbe999ab6406528ffe48f1","maintainer":false,"links":[]}]}'),p={name:"guide/repaint-optimization.md"};function d(m,e,u,g,f,b){return t(),i("div",null,[...e[0]||(e[0]=[s('<h1 id="repaint-optimization" tabindex="-1">Repaint optimization <a class="header-anchor" href="#repaint-optimization" aria-label="Permalink to &quot;Repaint optimization&quot;">​</a></h1><h2 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to &quot;Introduction&quot;">​</a></h2><p>Two basic controls for on screen display exist in Graphics32: <code>TCustomPaintBox32</code> and <code>TCustomImage32</code>. These two classes provide the functionality all other graphical controls in Graphics32 are based on.</p><div class="info custom-block"><p class="custom-block-title">Note</p><p>While this article focuses on the <code>TCustomPaintBox32</code> and <code>TCustomImage32</code> base classes. The problems and solutions they outline extends to the derived controls that you would actually use, such as <code>TPaintBox32</code>, <code>TImage32</code>, and <code>TImgView32</code>.</p></div><h3 id="double-buffering-in-tcustompaintbox32" tabindex="-1">Double buffering in TCustomPaintBox32 <a class="header-anchor" href="#double-buffering-in-tcustompaintbox32" aria-label="Permalink to &quot;Double buffering in TCustomPaintBox32&quot;">​</a></h3><p><code>TCustomPaintBox32</code> implements a control similar to the <code>TPaintBox</code> control known from Delphi’s Visual Component Library (VCL). It differs from the latter in the way it handles repaints: While <code>TPaintBox</code> directly draws to the display context whenever it needs to update, <code>TCustomPaintBox32</code> uses an in-memory backbuffer. This technique, generally called <a href="https://en.wikipedia.org/wiki/Multiple_buffering#Double_buffering_in_computer_graphics" target="_blank" rel="noreferrer"><em>double buffering</em></a>, has its up and downsides: While it provides a convenient and simple way to avoid flickering by reducing many on-screen paint operations to just one synchronized buffer transfer (blit) from memory to screen, it also require a significant amount of memory and bus bandwidth which in turn effectively limits the number of possible updates per second depending on the hardware used. The main problem with a too simplistic approach to double buffering, is that the whole buffer is transferred to screen even if just a small fraction of its area has changed. Thus there is a lot of potential to improve on - more on this later.</p><h3 id="repaints-in-tcustomimage32" tabindex="-1">Repaints in TCustomImage32 <a class="header-anchor" href="#repaints-in-tcustomimage32" aria-label="Permalink to &quot;Repaints in TCustomImage32&quot;">​</a></h3><p>The <code>TCustomImage32</code> control extends <code>TCustomPaintBox32</code> by replacing the direct painting to the back buffer with so called stacked <a href="./using-timage32/paint-stages"><em>Paint Stages</em></a>. Upon repaint these paint stages are executed in a succesive fashion, from bottom to top – each stage drawing onto the result of the previous to produce the final output.</p><p><img src="'+n+'" alt="Paintstages at runtime"><strong>Figure 1:</strong> Paintstages at runtime</p><p>Once a change happens in this stack, at any given stage, a deferred invalidation of the whole back buffer content is triggered, which in turn leads to a complete repaint of all stages once this invalidation request is handled by the application message queue. This is where the main problem resides: Even with the smallest change (e.g. updating a single pixel in a layer) the whole buffer area needs to be repainted. This approach, though simple, is naive and results in unnecessary CPU and bus- and memory-bandwidth utilization.</p><hr><p>To sum up, we have two main problems to overcome, i.e. to optimize away:</p><ul><li>Forced full scene repaint of paint stages to back buffer in <code>TCustomImage32</code>.</li><li>Forced full scene repaint from back buffer to screen in <code>TCustomPaintBox32</code>.</li></ul><h2 id="the-repaint-optimizer" tabindex="-1">The Repaint Optimizer <a class="header-anchor" href="#the-repaint-optimizer" aria-label="Permalink to &quot;The Repaint Optimizer&quot;">​</a></h2><p>In order to resolve the two problems outline above, <code>TCustomPaintBox32</code> and <code>TCustomImage32</code> employs an internal <em>repaint optimizer</em> object that takes care of the aspect of managing and optimizing changed areas.</p><p>To control how updates and repaint are handled, <code>TCustomPaintBox32</code> (and by inheritance <code>TCustomImage32</code>) provides the RepaintMode property. <code>RepaintMode</code> controls what algorithm is used to manage repaints:</p><ul><li><strong><code>rmFull</code></strong> means that no repaint optimization will be used; Every change, no matter how small, produces a full scene repaint.</li><li><strong><code>rmOptimizer</code></strong> means that the repaint manager is used to repaint only updated areas.</li></ul><p><img src="'+o+`" alt=""><strong>Figure 2:</strong> Comparison between full scene and optimized repaint</p><p>Figure 2 shows an example comparison of a <code>rmFull</code> full scene repaint and an <code>rmOptimizer</code> optimized repaint, for simple layer operations like moving or resizing. It can be seen that the <code>rmOptimizer</code> method breaks the full scene repaint down to just a fractional repaint - namely those parts that were changed. Both modes are used in <code>TCustomPaintBox32</code> for repaint to screen and in <code>TCustomImage32</code> for repaint to buffer.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>In addition to <code>rmFull</code> and <code>rmOptimizer</code>, there is an addition repaint mode named <code>rmDirect</code> which provides a method for direct repainting to screen. In this mode the deferred repaint technique is replaced by an immediate repaint. The use cases for this technique are very limited, but is especially useful for something like the <code>TSyntheticImage</code> class, which provides incremental painting of the result while still rendering.</p></div><h2 id="measuring-mode" tabindex="-1">Measuring Mode <a class="header-anchor" href="#measuring-mode" aria-label="Permalink to &quot;Measuring Mode&quot;">​</a></h2><p>Layers in Graphics32 are a special case that needs to be taken care of separately; Since layers are not forced to stay within their determined bounds (for <code>TPositionedLayer</code> for example), they can basically paint everywhere on the buffer. Thus we need to find some other way of determining which areas the layer is drawing to. In order to enable this, all safe drawing operations in Graphics32 supports a mechanism called measuring. This method can basically be thought of as a simulation mode or dry-run where nothing is actually drawn to the buffer but the target bitmap is still notified of what <em>would</em> have been changed. So, this way the repaint optimizer can get information of which areas the operation is drawing to. As a matter of fact, the repaint optimizer just needs to iterate through all marked layers (compare <em>Figure 2</em>), calling the Paint method of each layer with the measuring mode enabled. The information gathered in this process is used for the repaint manager’s internal optimization work, ie. unifying overlapping areas and minimizing the number of rectangles to be updated.</p><p>Profiling has shown, that the measuring process adds only negligible overhead to the repaint process. However, the developer needs to take care of certain facts in his custom code in order to actually take advantage of the performance benefits the repaint optimizer offers.</p><div class="half custom-block"><p><strong>Code 1</strong></p><div class="language-pascal vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">pascal</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">begin</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  MyDrawingOperation(Buffer);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Buffer.Changed;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div></div><div class="half custom-block"><p><strong>Code 2</strong></p><div class="language-pascal vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">pascal</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">begin</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> not</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Buffer.Measuringmode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">then</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    MyDrawingOperation(Buffer);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  Buffer.Changed(RectOfAreaThatWasChanged);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div></div><p>Code 1 compared to Code 2 illustrates the required changes in pseudo-Pascal-code. As seen in Code 2, a simple check for active measuring mode is introduced. In this case the actual drawing operation is omitted, but the change notification is still done.</p><p>If the developer’s code includes calls to the <code>Changed</code> method, those calls needs to be changed to only represent the <em>changed area</em> instead of the whole buffer area. Keeping the <code>Changed</code> method unmodified will force a complete buffer invalidation, thus the effect of the repaint optimizer and the partial repaint therefore is effectively annulled. Also, the custom code needs to be fully safe, meaning it has to offer full clipping support.</p><p>If the developer’s custom code solely relies on the safe drawing operations provided by Graphics32, there is no need to change this code since those functions already handle measuring mode internally. However, doing so will likely result in better performance especially if the custom code is calling many safe drawing operations. In this case introducing the changes of <em>Code 2</em> could simplify the measuring process a lot by overriding all subordinated checks by one superordinate check for measuring mode.</p><p>So, to sum up, there are two possible pitfalls in custom code that can occur with the optimized repaint approach:</p><ol><li><code>Changed</code> calls need to be taken care of (or else the whole buffer area is repainted).</li><li>Custom code has to be clippable, ie. needs to obey the buffer’s <code>ClipRect</code> property (or else visual artifacts and failures appear).</li></ol><h2 id="internals" tabindex="-1">Internals <a class="header-anchor" href="#internals" aria-label="Permalink to &quot;Internals&quot;">​</a></h2><p>As already mentioned above, the repaint optimizer is responsible for managing and optimizing changed areas, which are described by rectangles. Because there can be quite a lot changes happening between repaints, the area information has to be saved in a space-saving and performance-optimal structure.</p><p>The naive approach of saving all rectangles into a list and combining them, once the repaint optimizer must determine what to repaint, is not suitable. With each <code>TRect</code> instance being 16 byte in size, the memory usage is unacceptable for large sets and the overhead of reallocating such structures is also noticeable. Moreover one has to make sure not to add several overlapping rectangles to the list as using an algorithm to handle this matter adds complexity to the process. Thus, a better and more flexible way of managing possibly overlapping rectangles must be used.</p><h3 id="dirty-rectangles" tabindex="-1">Dirty rectangles <a class="header-anchor" href="#dirty-rectangles" aria-label="Permalink to &quot;Dirty rectangles&quot;">​</a></h3><p>One approach to the &quot;dirty rectangles&quot; problem is to subdivide the buffer’s dimension into a matrix; Each cell or tile of this matrix is responsible for a 32x32 pixel area in the buffer. New rectangles are simply rendered to this matrix. The memory usage stays constant because the matrix size is in fixed relation to the bitmap buffer size. Also, the problem of handling overlapping rectangles is also easily solved by rendering to the matrix. Additionally, unifying tiles to bigger rectangles is obviously less complex than the approach needed for determining and unifying rectangles from a list structure. However, because each tile of our matrix only holds a binary value (filled or empty), the granularity (compare Figure 3) of this approach is quite high and thus too much information is lost.</p><p><img src="`+r+'" alt=""><strong>Figure 3:</strong> Granularity comparison of MicroTiles and Tiles</p><h3 id="micro-tiles" tabindex="-1">Micro Tiles <a class="header-anchor" href="#micro-tiles" aria-label="Permalink to &quot;Micro Tiles&quot;">​</a></h3><p>The solution used by the Graphics32 repaint optimizer is based on the tile method but resolves the granularity problem by expanding each tile in the matrix from a binary representation to an integer representation.</p><p><img src="'+l+'" alt=""><strong>Figure 4:</strong> Rectangle rendered to 32 x 32 Pixel MicroTiles</p><p>So, instead of only having to restrict to <em>full</em> or <em>empty</em> as possible values, the tile contains exactly one rectangle that can further define the content. The two 16-bit values in the 32-bit integer of each tile represent the upper left and lower right corner of the inscribed rectangle relative to the upper left position of the tile (Figure 4). This allows a finer granularity and in the worst case (tile completely filled with one inscribed rectangle) the solution equals the tile based approach. However, most times the result is better, thus more information about the original shape is kept.</p><p>Because each coordinate is 8-bit wide, the tile size can scale up to 256 x 256 in size.</p><p>This method was first implemented by the developer of <a href="http://www.levien.com/libart/" target="_blank" rel="noreferrer">libart</a> by the name <a href="http://www.levien.com/libart/uta.html" target="_blank" rel="noreferrer">MicroTile Arrays</a>. Graphics32 implements an optimized version of its own and mixes that with some specialities: The MicroTiles Repaint Optimizer implements a simple adaptive algorithm that chooses between full scene, tile and MicroTile based operation mode depending on the current update situation.</p><p>For instance with many small rectangles (500+) the MicroTiles based optimization becomes less effective and can impose a performance penalty. In this case the adaptive algorithm will automatically downgrade to the next lower mode, which in that situation is the tiles based mode. Because the granularity is bigger in this mode, the optimization process is also less complex. Once the situation normalizes, it switches back to MicroTiles based operation mode. Thus, a good performance should be guaranteed in almost all cases.</p><h2 id="benchmarks" tabindex="-1">Benchmarks <a class="header-anchor" href="#benchmarks" aria-label="Permalink to &quot;Benchmarks&quot;">​</a></h2><p>The <code>Sprites_Ex</code> project was the most important performance test case of all because it is exceptional in the way that it shows both the strengths and weaknesses of the MicroTiles based approach. For our tests we’ve extended the project slightly to be able to measure the effective frames (or updates) per second.</p><p><img src="'+h+'" alt=""> <img src="'+c+'" alt=""><strong>Figure 5:</strong> Benchmark results with Sprites_Ex</p><p>Figure 5 shows two results of benchmarking with different canvas resolutions. Each bitmap layer has a size of either 32 x 32 or 64 x 64 pixel picked randomly. The random seed used in the benchmark is reproducible for each test machine, thus a valid comparison is possible.</p><p>As seen in the first graph (a) the MicroTiles based optimization works considerable better than the Tiles based approach. However, on our test machine it becomes less effective starting with 70 changed layers and finally the Tiles based approach outpaces it starting from 130 changed layers, because the higher granularity helps while combining the tiles to uniform rectangles. Using the MicroTiles based approach results in too many rectangles, which in this situation are less effective than fewer combined rectangles. This trend continues up to 600 rectangles. The Tiles based approach finally converges against the full scene repaint with MicroTiles being slightly worse due to the overhead involved. With so many layers the canvas area is almost completely covered with updates.</p><p>For Graph (b) the results are slightly shifted due to the bigger canvas size. Both graphs show that the adaptive approach works well enough to be a feasible solution, however, the overhead of the balancing and scheduling used is noticeable in the switching regions of the graphs.</p>',49)])])}const v=a(p,[["render",d]]);export{w as __pageData,v as default};
