import{_ as a,b as t,e as s,a8 as o}from"./chunks/framework.CD0vREyi.js";const m=JSON.parse('{"title":"Using layers","description":"","frontmatter":{"date":1787742964000,"authors":[],"tags":[],"head":[]},"headers":[],"relativePath":"guide/using-timage32/using-layers.md","filePath":"guide/using-timage32/using-layers.md","lastUpdated":1787742964000,"timestamp":1787742964000,"datetime":"2026-08-26T11:16:04.000Z","contributors":[{"commits":1,"email":"anders@melander.dk","name":"Anders Melander","avatar":"https://gravatar.com/avatar/a6bff52a3ccbe999ab6406528ffe48f1","maintainer":false,"links":[]}]}'),i={name:"guide/using-timage32/using-layers.md"};function r(n,e,l,h,c,d){return t(),s("div",null,[...e[0]||(e[0]=[o('<h1 id="using-layers" tabindex="-1">Using layers <a class="header-anchor" href="#using-layers" aria-label="Permalink to &quot;Using layers&quot;">​</a></h1><p>The <code>TImage32</code> control contains an indexed collection of layers, referenced by the <a href="/api/GR32_Image/TCustomImage32/Properties/Layers">Layers</a> property. The number of layers is limited only by the amount of free memory.</p><p>Each layer is an entity, which basically ‘knows’ how to paint itself onto the back-buffer of the control and how to interact with the mouse. Layers are indexed and their indexes are similar to the Z-order of standard controls. Layers with smaller indexes are considered to lie deeper.</p><h2 id="common-properties-and-methods" tabindex="-1">Common Properties and Methods <a class="header-anchor" href="#common-properties-and-methods" aria-label="Permalink to &quot;Common Properties and Methods&quot;">​</a></h2><p>All layers inherit from the <a href="/api/GR32_Layers/TCustomLayer">TCustomLayer</a> class. This class implements behavior and properties common to all layer types.</p><p>The basic layer’s behavior is controlled by its <a href="/api/GR32_Layers/TCustomLayer/Properties/LayerOptions">LayerOptions</a> property, which is a 32-bit unsigned integer value composed of <a href="/api/GR32_Layers/Constants/Layer Options Bits">Layer Options Bits</a>. This property allows for fast and relatively simple referencing of layers and groups of layers.</p><p>Consider, for example, the <code>LOB_VISIBLE</code> bit (31-st bit in layer options). When <code>TImage32</code> repaints its layers at the <code>PST_DRAW_LAYERS</code> stage, it uses the stage parameter from corresponding <a href="./Paint-Stages#TPaintStage Record">TPaintStage record</a> as a bit-mask (default value is <code>LOB_VISIBLE=$80000000</code>). Being compared (logical AND operation) with <a href="/api/GR32_Layers/TCustomLayer/Properties/LayerOptions">LayerOptions</a> of each layer, this mask determines whether the layer should be painted or not.</p><p>Similar situation is with reaction to mouse messages (see below).</p><p>Using <a href="/api/GR32_Layers/TCustomLayer/Properties/LayerOptions">LayerOptions</a> you can easily customize the appearance and modify the order in which <code>TImage32</code> repaints layers. For example, you may assign several categories to layers, using lower 24-bits of <a href="/api/GR32_Layers/TCustomLayer/Properties/LayerOptions">LayerOptions</a> and then just change the mask in corresponding <a href="./Paint-Stages#TPaintStage Record">TPaintStage record</a> to specify exactly which category you want to be displayed.</p><p>Note that you can insert several <code>PST_DRAW_LAYERS</code> paint stages (even before the <code>PST_DRAW_BITMAP</code> stage), each with its own mask, or, alternatively, call <a href="/api/GR32_Image/TCustomImage32/Methods/ExecDrawLayers">ExecDrawLayers</a> while handling <code>PST_CUSTOM</code> stages. Basically it means, that you can realize almost any complex repainting scheme, like overlay layers, underlay layers, layers which are always on top or always in the background, etc.</p><h2 id="tracking-the-mouse" tabindex="-1">Tracking the Mouse <a class="header-anchor" href="#tracking-the-mouse" aria-label="Permalink to &quot;Tracking the Mouse&quot;">​</a></h2><p>Layers are capable of responding to mouse-down/move/up messages. When the layer container (the <code>TImage32</code> control) receives a mouse event it searches the layer collection, from top to bottom, for a single layer that can handle the mouse event.</p><p>In order for a layer to be eligible to receive mouse events the layer must satisfy both of the following conditions:</p><ul><li>The layer has <code>LOB_MOUSE_EVENTS</code> bit activated in its <a href="/api/GR32_Layers/TCustomLayer/Properties/LayerOptions">LayerOptions</a>.</li><li>The layer passes a hit test (see below).</li></ul><p>If a suitable layer is found then the event is passed on to the layer and the layer generates a corresponding OnMouseDown, OnMouseMove or OnMouseUp event.</p><p>Once the layer mouse handling is done the <code>TImage32</code> control continues its normal mouse handling and generate the relevant <a href="/api/GR32_Image/TCustomImage32/Events/OnMouseDown">OnMouseDown</a>, <a href="/api/GR32_Image/TCustomImage32/Events/OnMouseMove">OnMouseMove</a> or <a href="/api/GR32_Image/TCustomImage32/Events/OnMouseUp">OnMouseUp</a> event.</p><p>Note that layer mouse events can be completely disabled by setting the layer collection <a href="/api/GR32_Layers/TLayerCollection/Properties/MouseEvents">MouseEvents</a> property to <em>False</em>. When layer mouse events has been disabled, the <code>TImage32</code> control will generate mouse events right away, without checking for a possible receiving layer.</p><h2 id="layer-hit-testing" tabindex="-1">Layer hit testing <a class="header-anchor" href="#layer-hit-testing" aria-label="Permalink to &quot;Layer hit testing&quot;">​</a></h2><p>As mentioned, in order for a layer to be eligible to receive mouse events, the layer must pass a <em>hit test</em>. Hit testing is performed by calling the layer’s <a href="/api/GR32_Layers/TCustomLayer/Methods/HitTest">HitTest</a> method:</p><div class="language-pascal vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">pascal</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HitTest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(X, Y: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">): </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">virtual</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>The <strong>X</strong> and <strong>Y</strong> parameters here are the coordinates of the mouse pointer in pixels, relative to the top-left corner of the <code>TImage32</code> control. The function returns a value that indicates if the layer wants to process the mouse event.</p><p>By default <code>HitTest</code> returns True regardless of the mouse coordinates but most layer types override <code>HitTest</code> to take the layer content and position into account. For example the <code>TPositionedLayer</code> layer type only returns <code>True</code> if the mouse position is within the layer bounds and the <a href="/api/GR32_Layers/TBitmapLayer">TBitmapLayer</a> type extends this to optionally also take the transparency of its bitmap into account; Clicks or movement over fully transparent bitmap pixels does not generate mouse events.</p><p>The built in hit test logic can easily be customized using the layer’s <a href="/api/GR32_Layers/TCustomLayer/Events/OnHitTest">OnHitTest</a> event.</p><h2 id="mouse-capture" tabindex="-1">Mouse capture <a class="header-anchor" href="#mouse-capture" aria-label="Permalink to &quot;Mouse capture&quot;">​</a></h2><p>Similar to standard controls, layers can also <em>capture</em> mouse messages.</p><p>Mouse capture is necessary in order for a control to continue to receive mouse messages even after the mouse leaves the control. For example, without mouse capture, if a user clicked on a control and then moved the mouse pointer out of the control and released the mouse button, the control would only receive a mouse-down message and mouse-move messages until the mouse left the control. It would never receive the mouse-up message. With mouse capture the control will receive mouse messages until it releases the mouse capture.</p><p>By default, a layer that has satisfied the hit test criteria mentioned above, will automatically acquire mouse capture when the layer is clicked and release mouse capture when the mouse button is released. A layer can disable automatic mouse capture by setting the <code>LOB_NO_CAPTURE</code> layer option. This is commonly done for layers that are visible but which should not interact with the mouse.</p><h2 id="painting-layers" tabindex="-1">Painting Layers <a class="header-anchor" href="#painting-layers" aria-label="Permalink to &quot;Painting Layers&quot;">​</a></h2><p>Layers are painted in bottom-to-top order, starting from lower indexes. By default, only visible layers are painted, that is the ones with <code>LOB_VISIBLE</code> bit set, but as it was shown above, this order may be changed.</p><h2 id="positioned-layers" tabindex="-1">Positioned Layers <a class="header-anchor" href="#positioned-layers" aria-label="Permalink to &quot;Positioned Layers&quot;">​</a></h2><p>Positioned layers are the layers you will probably use most of the time. The base class for positioned layers is <a href="/api/GR32_Layers/TPositionedLayer">TPositionedLayer</a>. In addition to basic layer behavior, it introduces a <a href="/api/GR32_Layers/TPositionedLayer/Properties/Location">Location</a> property (of the <a href="/api/GR32/Types/Rectangle Types#TFloatRect">TFloatRect</a> type), which specifies the layer’s position and size. Having location specified as a floating point rectangle helps to avoid rounding errors when layers are resized.</p><p>The location can be specified in pixels, relative to the <code>TImage32</code> control’s top-left corner, or in scaled pixels, relative to the top-left corner of the <a href="./bitmap-image">bitmap image</a>. In the second case, the scale of the layer coincides with the scale of the bitmap image and the actual location of the layer relative to the top-left corner of <code>TImage32</code> may be obtained with its <a href="/api/GR32_Layers/TPositionedLayer/Methods/GetAdjustedLocation">GetAdjustedLocation</a> method.</p>',32)])])}const y=a(i,[["render",r]]);export{m as __pageData,y as default};
